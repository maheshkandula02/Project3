To check with the swagger in the browser:

http://localhost:9900/swagger-ui.html


To check with the H2 database:

http://localhost:9900/h2 (after clicking on this we see the test and connect screen and the give the 

url: jdbc:h2:mem:testdb and then click on test connection and then connect) .

Finally , we can see the h2 database
